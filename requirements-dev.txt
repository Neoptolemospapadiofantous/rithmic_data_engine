# Development and testing dependencies
pytest>=7.0
pytest-xdist>=3.0         # parallel test execution (-n auto)
pytest-timeout>=2.0        # per-test timeouts
anyio>=4.0                 # async test support (pytest-anyio marker in pytest.ini)
flask>=3.0                 # web UI for live dashboard
mypy>=1.0                  # static type checking (used by make hermes)
ruff>=0.4                  # linter / formatter (used by make hermes)
